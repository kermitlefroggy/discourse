rvm:
  - 2.1.2

service:
 - redis-server
 
before_install:
 - bundle install
 - bundle exec rake db:create
 - bundle exec rake db:migrate
 
# Running npm test to run your test cases
script:
 - bundle exec rspec
